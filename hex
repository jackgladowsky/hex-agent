#!/bin/bash
# Hex launcher
DIR="$(dirname "$(readlink -f "$0")")"

# Use compiled version if available, otherwise use tsx
if [ -f "$DIR/dist/cli.js" ]; then
    node "$DIR/dist/cli.js" "$@"
else
    npx tsx "$DIR/src/cli.tsx" "$@"
fi
